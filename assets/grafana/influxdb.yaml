apiVersion: 1

datasources:
  - name: influxdb
    type: influxdb
    access: proxy
    database: db0
    #user: grafana
    url: http://localhost:8086
    jsonData:
      httpMode: GET
    #secureJsonData:
    #  password: grafana

